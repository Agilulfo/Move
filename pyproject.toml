[project]
name = "move"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "geopy>=2.4.1",
    "gpx>=2026.3.0",
]

[dependency-groups]
dev = [
    "ipdb>=0.13.13",
    "ipython>=9.15.0",
    "pytest>=9.1.1",
]
